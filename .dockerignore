# VCS / IDE
.git
.vscode
.idea

# Build outputs / generated artifacts
build
build-*
cmake-build-*
out
offline_release
packageResults_*
Upload_Result

# Large sample data (optional; keep if you need them inside image)
models
requests
tf_models

# Python envs / caches
**/__pycache__/
**/*.pyc

# Archives
**/*.zip
**/*.tar
**/*.tar.gz
**/*.tgz

# Keep ONNX runtime bundle (we NEED it in build context)
!docker/third_party/onnxruntime/**
!include/
!include/**
!docker/config.json